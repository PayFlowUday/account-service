# App
spring.application.name=account-service
server.port=8081

# MySQL
spring.datasource.url=jdbc:mysql://localhost:1624/payflow_account
spring.datasource.username=payflow
spring.datasource.password=payflow123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Eureka
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.instance.prefer-ip-address=true

# JWT
jwt.secret=payflow-super-secret-key-must-be-at-least-256-bits-long
jwt.expiration-ms=86400000

# Actuator
management.endpoints.web.exposure.include=health,info
